{
  "name": "my-project",
  "version": "1.0.0",
  "scripts": {
      "heroku-postbuild": "cd client && npm install && npm run build && cd ../server && npm install && cd .. && npm run clean:modules",
      "clean:modules": "rimraf client/node_modules server/node_modules"
  },
  "engines": {
      "node": ">=14.0.0"
  },
  "devDependencies": {
      "rimraf": "^6.0.1"
  }
}