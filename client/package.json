{
  "name": "client",
  "version": "0.1.0",
  "main": "App.js",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.17.0",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "axios": "^1.7.2",
    "faceapi": "^1.0.1",
    "package.json": "^0.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-player": "^2.16.0",
    "react-scripts": "5.0.0",
    "react-webcam": "^7.2.0",
    "serve": "^14.0.1",
    "socket.io-client": "^4.7.5",
    "web-vitals": "^2.1.4"
  },
  "devDependencies": {
    "@babel/plugin-proposal-private-property-in-object": "^7.21.11"
  },
  "scripts": {
    "start": "serve -s build",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "heroku-postbuild": "npm install && npm run build",
    "prestart": "node --openssl-legacy-provider ./node_modules/.bin/react-scripts start",
    "prebuild": "node --openssl-legacy-provider ./node_modules/.bin/react-scripts build"
  },
  "engines": {
    "node": "14.x"
  },
  "proxy": "http://localhost:8000/getResponse",
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
